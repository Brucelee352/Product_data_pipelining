from minio import Minio

client = Minio(
    "minio.brucea-lee.com",
    access_key="admin",
    secret_key="code_earth420",
    secure=True
)

try:
    if client.bucket_exists("sim-api-data"):
        print("Connected successfully!")
        print("Bucket exists!")
except Exception as e:
    print(f"Error: {e}")