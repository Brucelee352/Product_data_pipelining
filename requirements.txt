#===========================================================================
# Use pyproject.toml to install dependencies for both the main pipeline and
# the analytics queries.

# If using a virtual environment or your global environment,
# pyproject.toml can be ran with the following command:

# pip install -e .

#===========================================================================
# requirements.txt is used for the Dash app.
# Install with:

# pip install -r requirements.txt
#===========================================================================


# Requires Python 3.12.x
# Python 3.13+ breaks dbt-common via mashumaro's JSONObjectSchema regression.

# Core data handling
pandas>=3.0.1
numpy>=2.4.2

# Data formatting and storage
duckdb>=1.4.4
minio>=7.2.20
pyarrow>=23.0.1
fastparquet>=2025.12.0
dbt-duckdb>=1.10.1
dbt-adapters>=1.22.8
dbt-common>=1.37.3
dbt-core>=1.11.7
dbt-extractor>=0.6.0
dbt-semantic-interfaces>=0.9.0
# 3.13 and 3.14 have a self-referential regression in JSONObjectSchema
# that breaks dbt-common on import. Pinned to last known-good version.
mashumaro==3.12

# Data Visualization and Portfolio App
plotly>=6.0.0
dash>=2.18.0
dash-bootstrap-components>=1.6.0

# Data generation
Faker>=40.8.0
user-agents>=2.2.0

# Logging
python-dotenv>=1.2.2

# Date & time handling
python-dateutil>=2.9.0

# Optional: Development dependencies
# pytest>=7.0.0  # For testing
# black>=22.0.0  # For code formatting
