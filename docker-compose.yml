services:
  app:
    build:
      context: .
      secrets:
        - env
    ports:
      - "${PORT:-8050}:${PORT:-8050}"
    environment:
      PORT: "${PORT:-8050}"
    env_file:
      - path: .env
        required: false

secrets:
  env:
    file: .env
